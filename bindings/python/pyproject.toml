[build-system]
requires = ["scikit-build-core>=0.8.0", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "mars-core"
version = "0.1.0"
description = "Python bindings for mars-framework core"

[tool.scikit-build]
cmake.source-dir = "../../"
